type Question @model
  @auth(rules: [
    { allow: public, operations: [read, create, update, delete] }
  ]) {
  id: ID!
  question: String!
  choices: [String!]!
  answer: String!
  topic: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}

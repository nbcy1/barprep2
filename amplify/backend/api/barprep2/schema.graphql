type Question @model @auth(rules: [{ allow: public }]) {
  id: ID!
  question: String!          # was 'title'
  choices: [String!]!        # was missing
  answer: String!
  topic: String              # optional, for categories
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}

type Question @model
  @auth(rules: [
    { allow: public, operations: [read] },      # anyone can read
    { allow: private, operations: [create, update, delete] } # only creator can modify
  ]) {
  id: ID!
  text: String!
  answer: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}
